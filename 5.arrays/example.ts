var arrayNumbers:number[] = [1,2,3]; //numer[] -> This denotes the number array datatype
arrayNumbers=[2,5,6,7]; // we can update or change the value
//arrayNumbers=["one","two"]  // Type 'string' is not assignable to type 'number'.ts(2322)
